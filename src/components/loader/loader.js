import React from 'react';
import CircularProgress from '@mui/material/CircularProgress';

const Loader = () => {
    return (
        <CircularProgress sx={{color:'#fb7650'}} />
    );
};

export default Loader;